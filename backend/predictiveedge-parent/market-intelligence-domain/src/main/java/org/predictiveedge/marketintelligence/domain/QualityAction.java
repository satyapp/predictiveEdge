package org.predictiveedge.marketintelligence.domain;

/** Direct policy action; this is evaluated independently of dimension scores. */
public enum QualityAction {
    ALLOW,
    DEGRADE,
    BLOCK
}
