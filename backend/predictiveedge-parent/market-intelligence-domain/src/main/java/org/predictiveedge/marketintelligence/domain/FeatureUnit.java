package org.predictiveedge.marketintelligence.domain;

/** Canonical unit carried with every feature value. */
public enum FeatureUnit {
    PRICE,
    PERCENT,
    RATIO,
    INDEX_POINTS,
    VOLUME,
    DIMENSIONLESS
}
